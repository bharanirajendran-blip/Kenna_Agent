# =============================================
# Kenna Remediation Agent — Configuration
# =============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git

# LLM Settings (OpenAI)
OPENAI_API_KEY=YOUR_KEY_HERE
OPENAI_MODEL=gpt-4o
OPENAI_TEMPERATURE=0.1

# Input/Output Paths (defaults work out of the box)
KENNA_INPUT_PATH=data/kenna_input_sanitized.json
PROMPT_PATH=prompt_template.md

# ---- Optional: Email Notification ----
# Leave blank to skip email step
SMTP_HOST=
SMTP_PORT=25
SMTP_STARTTLS=0
SMTP_USER=
SMTP_PASS=
REPORT_SENDER=
REPORT_RECIPIENTS=
