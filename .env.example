# =============================================
# Kenna Remediation Agent — Configuration
# =============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git

# LLM Settings (OpenAI)
OPENAI_API_KEY=YOUR_KEY_HERE
OPENAI_MODEL=gpt-4o
OPENAI_TEMPERATURE=0.1

# Input/Output Paths (defaults work out of the box)
# Source mode:
#   csv -> read normalized JSON from KENNA_INPUT_PATH (default)
#   api -> fetch normalized JSON from Kenna-compatible API
KENNA_SOURCE=csv
KENNA_INPUT_PATH=data/kenna_input_sanitized.json
PROMPT_PATH=prompt_template.md

# ---- Optional: Kenna API Source (used when KENNA_SOURCE=api) ----
# Recommended for demos: point this to an endpoint that already returns
# normalized payload shape:
#   { "asset_group": "...", "fixes": [ ... ] }
KENNA_API_NORMALIZED_URL=
KENNA_API_TOKEN=
KENNA_API_TIMEOUT=20
# Optional offline API simulation (file path to normalized JSON)
KENNA_API_SNAPSHOT_PATH=

# ---- Optional: Email Notification ----
# Leave blank to skip email step
SMTP_HOST=
SMTP_PORT=25
SMTP_STARTTLS=0
SMTP_USER=
SMTP_PASS=
REPORT_SENDER=
REPORT_RECIPIENTS=
